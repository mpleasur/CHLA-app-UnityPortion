using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ActivateModal : MonoBehaviour
{
    //canvas variable
    public GameObject mysecondCanvas; 
    //show canvas     public void activateCanvas()     {         mysecondCanvas.SetActive(true);


    } 
    //if button is clicked -      public void onClickButton(string choice)     {
        //if button is clicked sends messsage continue         if (choice == "continue")         {
            //hides modal window             mysecondCanvas.SetActive(false);         }     }   
}
